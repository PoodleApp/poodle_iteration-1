/* @flow */

/**
 * The idea with this module is that it produces serialized requests that will
 * be processed in the main process.
 */

// function getContent(uri: URI): Promise<{ contentType: string, content: Buffer }[]> {
//   // TODO
// }
